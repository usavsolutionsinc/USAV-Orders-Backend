# dependencies
node_modules/
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build
/desktop-dist
/desktop-dist2

# electron sidecar — uploaded files should not be committed
/server/storage/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
ts-debug.log*
firebase-debug.log
firebase-debug.*.log

# scratch / backups
snapshot*.txt
*.bak.*
.env.bak*
.env*.tmp
.env.nango
receiving_lines_cleanup_backup_*.json

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

certificates

# backfill scripts — local checkpoints and run logs
.backfill-*.state.json

# Dead code / hygiene reports (large or noisy)
reports/knip-report-*.json
reports/dead-code-*.md

# Previous Electron build artifact tree (should never be committed)
apps/desktop/

# PWA / service worker build outputs (generated)
public/sw.js
public/workbox-*.js
public/fallback-*.js
public/swe-worker-*.js
/logs/

# Manuals: source docx and converted PDF artifacts live in Vercel Blob now;
# these directories are intermediate output from scripts/convert-and-upload-manuals.mjs
/public/Manuals/
/converted-manuals/

docker-compose.db.yml

# Playwright E2E artifacts + saved auth storage state
/playwright-report/
/test-results/
/tests/.auth/
# Playwright MCP server scratch output (console logs + page snapshots)
.playwright-mcp/
.playwright/

# Local-only scratch script artifacts (studio verify runs, screenshots, one-off helpers)
scripts/_screenshots/
scripts/_verify-*
scripts/_mint-*
scripts/_fix-*
.env*
!.env.example

# Build installers (large artifacts; do not commit)
context/*.exe
*.exe
