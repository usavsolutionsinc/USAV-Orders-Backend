/**
 * Tenancy-guard route exemptions — the documented allowlist for invariant (A).
 *
 * GENERATED ONCE from the Phase-E route classification, then maintained BY HAND.
 * Each entry is a route that statically "touches" a FORCEd tenant table (per the
 * coarse word-match in scripts/tenancy-route-audit.mjs) yet is NOT GUC-wrapped —
 * and is exempt by design for the stated reason. Keyed by ROUTE PATH (stable as
 * more tables are FORCEd), never by table.
 *
 * Categories:
 *   preauth-identity      /api/auth/* identity/session lookup before a tenant
 *                         context exists (cross-org by necessity; cannot be GUC-scoped).
 *   cross-org-by-design   integration/sync/webhook/cron that legitimately spans orgs
 *                         on the owner pool (org resolved from payload or per-org sweep).
 *   no-db-false-positive  the route runs NO query on the flagged table; the word
 *                         matched an import/type/string/variable.
 *   helper-safe-delegation  tenant-table work is delegated to a GUC-aware src/lib/**
 *                         helper threaded ctx.organizationId; pool (if any) only for
 *                         ctx-stamped recordAudit / api-idempotency.
 *   owner-pool-admin      privileged admin path that must run on the owner pool.
 *
 * RATCHET: a NEW route that touches a FORCEd table and is neither GUC-wrapped nor
 * listed here FAILS the guard — add it here (with an honest reason) only after
 * confirming it is genuinely exempt, or GUC-wrap it instead.
 */

export type ExemptionCategory =
  | 'preauth-identity'
  | 'cross-org-by-design'
  | 'no-db-false-positive'
  | 'helper-safe-delegation'
  | 'owner-pool-admin';

export const ROUTE_TENANCY_EXEMPTIONS: Record<string, { reason: string; category: ExemptionCategory }> = {
  '/api/admin/org/delete': { category: 'owner-pool-admin', reason: "org soft-delete + session revoke on the organizations root table; owner-pool by design (step-up + confirm-slug gated)" },
  '/api/admin/organization/settings': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/admin/photos/mirror': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/admin/po-gmail/preview-unread': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/admin/po-gmail/reconcile': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/admin/staff/[id]/enroll-token': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/ai/chat': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/ai/chat/stream': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/ai/search': { category: 'no-db-false-positive', reason: "calls the Hermes AI gateway only; \"messages\" never queried (no DB)" },
  '/api/audit-log/packing': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/audit-log/receiving': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/audit-log/sku': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/audit-log/staff': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/audit-log/tech': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/audit-log/trace': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/audit/bin/[id]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/audit/sku/[sku]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/auth/account/passkey': { category: 'preauth-identity', reason: "/api/auth identity/session resolution before a tenant context exists; the cross-org staff/session/token lookup cannot be org-GUC-scoped" },
  '/api/auth/account/passkey/authenticate/finish': { category: 'preauth-identity', reason: "/api/auth identity/session resolution before a tenant context exists; the cross-org staff/session/token lookup cannot be org-GUC-scoped" },
  '/api/auth/account/passkey/register/finish': { category: 'preauth-identity', reason: "/api/auth identity/session resolution before a tenant context exists; the cross-org staff/session/token lookup cannot be org-GUC-scoped" },
  '/api/auth/email-login/request': { category: 'preauth-identity', reason: "/api/auth identity/session resolution before a tenant context exists; the cross-org staff/session/token lookup cannot be org-GUC-scoped" },
  '/api/auth/email-login/verify': { category: 'preauth-identity', reason: "/api/auth identity/session resolution before a tenant context exists; the cross-org staff/session/token lookup cannot be org-GUC-scoped" },
  '/api/auth/enroll/[token]': { category: 'preauth-identity', reason: "/api/auth identity/session resolution before a tenant context exists; the cross-org staff/session/token lookup cannot be org-GUC-scoped" },
  '/api/auth/invitation/accept': { category: 'preauth-identity', reason: "/api/auth identity/session resolution before a tenant context exists; the cross-org staff/session/token lookup cannot be org-GUC-scoped" },
  '/api/auth/passkey/authenticate/begin': { category: 'preauth-identity', reason: "/api/auth identity/session resolution before a tenant context exists; the cross-org staff/session/token lookup cannot be org-GUC-scoped" },
  '/api/auth/passkey/authenticate/finish': { category: 'preauth-identity', reason: "/api/auth identity/session resolution before a tenant context exists; the cross-org staff/session/token lookup cannot be org-GUC-scoped" },
  '/api/auth/passkey/register/begin': { category: 'preauth-identity', reason: "/api/auth identity/session resolution before a tenant context exists; the cross-org staff/session/token lookup cannot be org-GUC-scoped" },
  '/api/auth/passkey/register/finish': { category: 'preauth-identity', reason: "/api/auth identity/session resolution before a tenant context exists; the cross-org staff/session/token lookup cannot be org-GUC-scoped" },
  '/api/auth/pin': { category: 'preauth-identity', reason: "/api/auth identity/session resolution before a tenant context exists; the cross-org staff/session/token lookup cannot be org-GUC-scoped" },
  '/api/auth/pin/create': { category: 'preauth-identity', reason: "/api/auth identity/session resolution before a tenant context exists; the cross-org staff/session/token lookup cannot be org-GUC-scoped" },
  '/api/auth/session': { category: 'preauth-identity', reason: "/api/auth identity/session resolution before a tenant context exists; the cross-org staff/session/token lookup cannot be org-GUC-scoped" },
  '/api/auth/signin': { category: 'preauth-identity', reason: "/api/auth identity/session resolution before a tenant context exists; the cross-org staff/session/token lookup cannot be org-GUC-scoped" },
  '/api/auth/signout': { category: 'preauth-identity', reason: "/api/auth identity/session resolution before a tenant context exists; the cross-org staff/session/token lookup cannot be org-GUC-scoped" },
  '/api/auth/signup': { category: 'preauth-identity', reason: "/api/auth identity/session resolution before a tenant context exists; the cross-org staff/session/token lookup cannot be org-GUC-scoped" },
  '/api/auth/staff-picker': { category: 'preauth-identity', reason: "/api/auth identity/session resolution before a tenant context exists; the cross-org staff/session/token lookup cannot be org-GUC-scoped" },
  '/api/auth/step-up': { category: 'preauth-identity', reason: "/api/auth identity/session resolution before a tenant context exists; the cross-org staff/session/token lookup cannot be org-GUC-scoped" },
  '/api/auth/switch': { category: 'preauth-identity', reason: "/api/auth identity/session resolution before a tenant context exists; the cross-org staff/session/token lookup cannot be org-GUC-scoped" },
  '/api/auth/switch-org': { category: 'preauth-identity', reason: "/api/auth identity/session resolution before a tenant context exists; the cross-org staff/session/token lookup cannot be org-GUC-scoped" },
  '/api/billing/portal': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/billing/webhook': { category: 'cross-org-by-design', reason: "Stripe webhook; resolves the org from event metadata, then writes via org-scoped helpers" },
  '/api/bose-models': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/call-events': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/catalog/platform-accounts': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/catalog/platforms': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/catalog/platforms/[id]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/catalog/types': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/catalog/types/[id]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/checklists': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/cron/amazon/orders-sync': { category: 'cross-org-by-design', reason: "cross-org cron/webhook on the owner pool; the org is resolved or enumerated per payload/sweep (forEachActiveOrg/forEachOrg or a verified webhook), not a single ctx org" },
  '/api/cron/cleanup': { category: 'cross-org-by-design', reason: "cross-org cron/webhook on the owner pool; the org is resolved or enumerated per payload/sweep (forEachActiveOrg/forEachOrg or a verified webhook), not a single ctx org" },
  '/api/cron/integrations/sync': { category: 'cross-org-by-design', reason: "cross-org cron/webhook on the owner pool; the org is resolved or enumerated per payload/sweep (forEachActiveOrg/forEachOrg or a verified webhook), not a single ctx org" },
  '/api/cron/inventory/drift-check': { category: 'cross-org-by-design', reason: "cross-org cron/webhook on the owner pool; the org is resolved or enumerated per payload/sweep (forEachActiveOrg/forEachOrg or a verified webhook), not a single ctx org" },
  '/api/cron/photos/analyze': { category: 'cross-org-by-design', reason: "cross-org cron/webhook on the owner pool; the org is resolved or enumerated per payload/sweep (forEachActiveOrg/forEachOrg or a verified webhook), not a single ctx org" },
  '/api/cron/photos/drive-mirror': { category: 'cross-org-by-design', reason: "cross-org cron/webhook on the owner pool; the org is resolved or enumerated per payload/sweep (forEachActiveOrg/forEachOrg or a verified webhook), not a single ctx org" },
  '/api/cron/photos/nas-mirror': { category: 'cross-org-by-design', reason: "cross-org cron/webhook on the owner pool; the org is resolved or enumerated per payload/sweep (forEachActiveOrg/forEachOrg or a verified webhook), not a single ctx org" },
  '/api/cron/documents/nas-mirror': { category: 'cross-org-by-design', reason: "cross-org cron on the owner pool; selects GCS-primary outbound documents across orgs for NAS cold mirror" },
  '/api/cron/receiving/incoming-tracking-sync': { category: 'cross-org-by-design', reason: "cross-org cron/webhook on the owner pool; the org is resolved or enumerated per payload/sweep (forEachActiveOrg/forEachOrg or a verified webhook), not a single ctx org" },
  '/api/cron/shipping/reconcile-delivered': { category: 'cross-org-by-design', reason: "cross-org cron/webhook on the owner pool; the org is resolved or enumerated per payload/sweep (forEachActiveOrg/forEachOrg or a verified webhook), not a single ctx org" },
  '/api/cron/shipping/sync-due': { category: 'cross-org-by-design', reason: "cross-org cron/webhook on the owner pool; the org is resolved or enumerated per payload/sweep (forEachActiveOrg/forEachOrg or a verified webhook), not a single ctx org" },
  '/api/cron/sku-catalog/refresh-suggestions': { category: 'cross-org-by-design', reason: "cross-org cron/webhook on the owner pool; the org is resolved or enumerated per payload/sweep (forEachActiveOrg/forEachOrg or a verified webhook), not a single ctx org" },
  '/api/cron/sourcing/scan': { category: 'cross-org-by-design', reason: "cross-org cron/webhook on the owner pool; the org is resolved or enumerated per payload/sweep (forEachActiveOrg/forEachOrg or a verified webhook), not a single ctx org" },
  '/api/cron/staff-goals/history': { category: 'cross-org-by-design', reason: "cross-org cron/webhook on the owner pool; the org is resolved or enumerated per payload/sweep (forEachActiveOrg/forEachOrg or a verified webhook), not a single ctx org" },
  '/api/cron/stock-alerts': { category: 'cross-org-by-design', reason: "cross-org cron/webhook on the owner pool; the org is resolved or enumerated per payload/sweep (forEachActiveOrg/forEachOrg or a verified webhook), not a single ctx org" },
  '/api/cron/workflow-node-stats': { category: 'cross-org-by-design', reason: "cross-org cron/webhook on the owner pool; the org is resolved or enumerated per payload/sweep (forEachActiveOrg/forEachOrg or a verified webhook), not a single ctx org" },
  '/api/cron/zoho/fulfillment-sync': { category: 'cross-org-by-design', reason: "cross-org cron/webhook on the owner pool; the org is resolved or enumerated per payload/sweep (forEachActiveOrg/forEachOrg or a verified webhook), not a single ctx org" },
  '/api/cron/zoho/orders-ingest-drain': { category: 'cross-org-by-design', reason: "cross-org cron/webhook on the owner pool; the org is resolved or enumerated per payload/sweep (forEachActiveOrg/forEachOrg or a verified webhook), not a single ctx org" },
  '/api/desktop-app/release': { category: 'no-db-false-positive', reason: "Vercel Blob + GitHub API only; \"orders\" never queried" },
  '/api/ecwid/order-search': { category: 'no-db-false-positive', reason: "Ecwid REST API only; table words are response-shape vars" },
  '/api/ecwid/products/search': { category: 'no-db-false-positive', reason: "Ecwid REST API only; table words are response-shape vars" },
  '/api/ecwid/transfer-orders': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/failure-modes': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/failure-modes/[id]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/favorites': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/favorites/[id]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/google-sheets/transfer-orders': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/handling-units': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/integrations/[provider]/sync': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/integrations/google-drive/callback': { category: 'cross-org-by-design', reason: "OAuth callback with no session; org recovered from the encrypted state payload (upsertIntegrationCredentials)" },
  '/api/integrations/google-drive/connect': { category: 'no-db-false-positive', reason: "builds the OAuth redirect, encrypts org into state; no query" },
  '/api/integrations/google-drive/health': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/integrations/nextiva/webhook/[token]': { category: 'cross-org-by-design', reason: "anonymous webhook; org resolved from the URL token (resolveOrgByNextivaWebhookToken)" },
  '/api/inventory/bins-overview': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/locations': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/locations/[barcode]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/locations/[barcode]/properties': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/locations/bulk': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/locations/register': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/manual-server/assign': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/manual-server/by-item': { category: 'no-db-false-positive', reason: "external manual-server HTTP fetch; no DB" },
  '/api/manual-server/unassigned': { category: 'no-db-false-positive', reason: "external manual-server HTTP fetch; no DB" },
  '/api/nas-config': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/nas-dev/[[...path]]': { category: 'no-db-false-positive', reason: "reads the SMB share over the network; no DB query" },
  '/api/nas-target/[target]/[[...path]]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/nas/[[...path]]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/need-to-order': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/need-to-order/[id]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/operations/saved-views': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/order-amendments/[id]/decision': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/orders/[id]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/orders/[id]/allocate': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/orders/[id]/tracking': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/orders/skip': { category: 'no-db-false-positive', reason: "deprecated no-op; performs no DB query" },
  '/api/orders/start': { category: 'no-db-false-positive', reason: "deprecated no-op; performs no DB query" },
  '/api/outbound/mark-staged': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/packerlogs/for-order': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/packing-logs/save-photo': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/packing-photos': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/packing/policy': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/part-compatibility': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/photos/[id]/labels': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/photos/analyze': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/photos/drive-backup': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/photos/image-types': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/photos/labels': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/photos/labels/[id]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/photos/labels/bulk-apply': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/photos/library': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/photos/links': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/photos/listing-gallery': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/photos/nas-backup': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/photos/share': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/photos/share-packs': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/photos/share-packs/[token]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/photos/share-packs/[token]/zip': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/picking/session': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/picking/session/[id]/complete': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/picking/session/[id]/confirm-pick': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/picking/session/[id]/short-pick': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/product-manuals': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/product-manuals/assign': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/product-manuals/sync': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/product-manuals/thumbnail': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/product-manuals/upload': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/product-manuals/upsert': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/realtime/token': { category: 'no-db-false-positive', reason: "mints an Ably token from pure channel-name builders; no DB" },
  '/api/reason-codes/[id]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/receiving-lines/[id]/ensure-catalog': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/receiving-lines/incoming/email-rescan': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/receiving-lines/incoming/refresh': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/receiving-lines/incoming/zoho-refresh': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/receiving-tasks': { category: 'no-db-false-positive', reason: "drizzle on receiving_tasks; \"receiving\" matched the route name, not the receiving table" },
  '/api/receiving/[id]/attach-box': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/receiving/disposition-suggest': { category: 'no-db-false-positive', reason: "LLM classifier (hermesToolCall); \"receiving\" never queried" },
  '/api/receiving/identify-label': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/receiving/lines/[id]/advance': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/receiving/nas-archive-test': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/receiving/pending-check': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/receiving/relink': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/receiving/unfound-queue/[kind]/[id]/push-to-zendesk/draft': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/receiving/visual-identify': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/receiving/zendesk-claim/archive-only': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/receiving/zendesk-claim/assist': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/receiving/zendesk-claim/assist-seller': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/receiving/zendesk-claim/classify': { category: 'no-db-false-positive', reason: "LLM classifier (hermesToolCall); \"receiving\" never queried" },
  '/api/receiving/zendesk-claim/draft': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/receiving/zendesk-claim/preview': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/receiving/zendesk-claim/seller-message': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/receiving/zendesk-claim/thread': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/repair-service': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/repair-service/[id]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/repair-service/[id]/link': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/repair/customers': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/repair/ecwid-categories': { category: 'no-db-false-positive', reason: "Ecwid categories API only; no DB" },
  '/api/repair/ecwid-products': { category: 'no-db-false-positive', reason: "Ecwid products API only; no DB" },
  '/api/repair/square-payment-link': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/replenish/bulk-create-po': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/replenishment/tasks/[id]/cancel': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/replenishment/tasks/[id]/claim': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/replenishment/tasks/[id]/complete': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/returns/intake': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/rma': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/rma/[id]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/rma/[id]/close': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/rma/[id]/disposition': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/rma/[id]/mark-received': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/rma/by-number/[number]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/rooms/[room]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/rooms/reorder': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/serial-units/[id]/list': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/serial-units/[id]/repairs': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/serial-units/[id]/test': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/serial-units/lookup': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/settings': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/shipped': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/shipped/[id]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/shipped/search': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/shipping/track/[id]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/shipping/track/register': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/shipping/track/sync-one': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/sku-catalog': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/sku-catalog/[id]/platform-ids': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/sku-catalog/graph/[skuId]/children': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/sku-catalog/graph/[skuId]/parents': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/sku-catalog/graph/[skuId]/tree': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/sku-catalog/pair-batch': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/sku-catalog/pair-ecwid': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/sku-catalog/suggest-pairings': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/sku-catalog/unpaired-ecwid': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/sourcing/alerts': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/sourcing/candidates': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/sourcing/candidates/[id]/import': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/sourcing/saved-searches': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/sourcing/saved-searches/[id]/run': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/sourcing/search': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/staff-goals/me': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/staff-messages': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/staff-preferences': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/staff-todos': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/staff/schedule/bulk': { category: 'no-db-false-positive', reason: "writes staff_weekly_schedule/overrides; \"staff\" matched strings, not the staff table" },
  '/api/studio/graph': { category: 'no-db-false-positive', reason: "drizzle reads workflow_* (not enforced); \"types\" matched the \"studio-types\" import path" },
  '/api/studio/items/[id]/recover': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/studio/templates': { category: 'no-db-false-positive', reason: "reads global workflow_templates; \"types\" matched the \"studio-types\" import path" },
  '/api/studio/templates/[id]': { category: 'no-db-false-positive', reason: "reads global workflow_templates; \"types\" matched the \"studio-types\" import path" },
  '/api/suppliers': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/suppliers/[id]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/support/overview': { category: 'no-db-false-positive', reason: "Zendesk API client (getZendeskSupportOverview); no DB" },
  '/api/tech/scan-repair-station': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/units/next-id': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/units/resolve-id': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/vision-config': { category: 'no-db-false-positive', reason: "returns an env var only; no DB" },
  '/api/voicemails/[id]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/voicemails/[id]/followup': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/voicemails/[id]/link': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/voicemails/[id]/recording': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/walk-in/catalog': { category: 'no-db-false-positive', reason: "Square API only (squareFetchForOrg); \"items\" is a local variable" },
  '/api/walk-in/customers': { category: 'no-db-false-positive', reason: "Square API only; \"customers\" is a local variable" },
  '/api/walk-in/orders': { category: 'no-db-false-positive', reason: "Square API only; \"orders\"/\"line_items\" are local variables" },
  '/api/walk-in/sales': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/walk-in/status': { category: 'no-db-false-positive', reason: "Square diagnostics only; no local query" },
  '/api/walk-in/sync': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/warranty/claims': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/warranty/claims/[id]/quote': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/warranty/claims/[id]/repair': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/warranty/claims/[id]/repair-handoff': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/warranty/claims/[id]/rma': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/warranty/claims/[id]/zendesk': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/warranty/claims/[id]/zendesk/link': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/warranty/claims/bulk': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/warranty/lookup': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/warranty/quotes/[id]': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/warranty/reports/export': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/webhooks/square': { category: 'cross-org-by-design', reason: "cross-org cron/webhook on the owner pool; the org is resolved or enumerated per payload/sweep (forEachActiveOrg/forEachOrg or a verified webhook), not a single ctx org" },
  '/api/webhooks/ups': { category: 'cross-org-by-design', reason: "cross-org cron/webhook on the owner pool; the org is resolved or enumerated per payload/sweep (forEachActiveOrg/forEachOrg or a verified webhook), not a single ctx org" },
  '/api/work-orders/calendar': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/zendesk/photo-ticket': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/zendesk/tickets': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/zendesk/tickets/[id]/assign': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/zendesk/tickets/[id]/comments': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/zendesk/tickets/[id]/photos': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/zoho/find-po': { category: 'no-db-false-positive', reason: "Zoho API only; \"purchase_order\" is a response var" },
  '/api/zoho/items/sync': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/zoho/oauth/authorize': { category: 'no-db-false-positive', reason: "builds the Zoho OAuth redirect from env; no DB" },
  '/api/zoho/purchase-orders': { category: 'no-db-false-positive', reason: "Zoho API only; response-shape vars, no query" },
  '/api/zoho/purchase-orders/sync': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/zoho/purchase-receives': { category: 'no-db-false-positive', reason: "Zoho API only; \"purchasereceives\" is a response var" },
  '/api/zoho/purchase-receives/import': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/zoho/purchase-receives/sync': { category: 'helper-safe-delegation', reason: "thin handler; tenant-table work is delegated to a src/lib/** helper threaded ctx.organizationId; pool (if imported) is used only for ctx-stamped recordAudit / api-idempotency — verified: no inline tenant pool.query" },
  '/api/zoho/warehouses': { category: 'no-db-false-positive', reason: "Zoho API only; \"warehouses\" is a response var" },
};
