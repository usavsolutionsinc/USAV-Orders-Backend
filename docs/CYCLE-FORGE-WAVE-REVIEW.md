# Cycle Forge — Roadmap Wave Review Dossier

> Generated by the orchestrator after an autonomous build + verification wave against `docs/CYCLE-FORGE-ROADMAP`.
> Status legend: ✅ done · 🔵 review (awaiting your approval) · ⬜ todo (needs your decision).
> **Integrated state at generation: full-repo `tsc` → 0 errors; route-permission manifest matches live source.**

---

## 1. How to use this doc
- Every task below is **additive + reversible** unless flagged. Most ran a Phase-0 discovery first and a lot turned out **already-built** (verify-only).
- Approve a task → flip its roadmap Status to `done`. Reject → `todo`/`blocked`.
- **Two things to eyeball before approving** (see §4): the one *applied* DB migration (PCK-02) and the regenerated route-permission manifest in your git diff.

---

## 2. Tasks at `review` (21) — one-line outcome + acceptance

### Foundations (P0)
| Task | Outcome | Acceptance |
|---|---|---|
| P0-IDN-01 | Serial-identity invariant **already enforced** (verified vs live DB: 0 dup `(org,serial)` groups). No code change. | A/B/C PASS |
| P0-TRACE-01 | Audit-event backbone (`inventory_events`); closed PUTAWAY org-attribution gap + widened order-timeline query; backfill script written. No migration. | A/B/C PASS |

### Trace spine (P1)
| Task | Outcome | Acceptance |
|---|---|---|
| P1-TRACE-02 | Universal timeline + serial↔order `groupMode` toggle; warranty panel migrated onto shared `EventTimeline`. | A/B/C PASS |
| P1-TRACE-03 | New first-trace view (`/audit-log/trace`); fixed Tech-missing-verdicts (anchor) + added actor to `readTimeline`. | A/B PASS |
| P1-TRACE-04 | Bell inbox **already wired** end-to-end (verified emit + once-per-carton guard). No code change. | A/B/C PASS |

### Packing / Testing / Work / Receiving (P1)
| Task | Outcome | Acceptance |
|---|---|---|
| P1-PCK-01 | Testing-mode SKU pre-pack scan branch (read-only resolve, `via:'sku'`). | A/B PASS |
| P1-PCK-02 | Per-SKU pack instructions + QC flags pre-confirm. **Fixed a live bug** (read of non-existent `sku_catalog.notes`). **⚠️ APPLIED a migration.** | A/B PASS |
| P1-PCK-03 | Testing feed defaults recently-received-first; reused live refresh (no new poll). | A/B PASS |
| P1-WORK-01 | Work-order assign **popover** + global-header top-priority chip; shared ranking lib. | A/B PASS |
| P1-WORK-02 | One shared `StaffFilter` + `?staff=` across Dashboard + Receiving rails. **Had a latent 500 bug — fixed in remediation (§3).** | A/B PASS |
| P1-WORK-03 | F2 hotkey → focus next-PO-serial scan (Playwright-proven). | A PASS |
| P1-RCV-01 | Searchable receiving to-do seeded from email order#s; reversible check-off. | A/B/C PASS |
| P1-MOB-01 | Mobile `/m/pack` two-step scan flow (typed reducer machine; reuses PCK-01 resolve). | A/B/C PASS |

### Verticals (P2)
| Task | Outcome | Acceptance |
|---|---|---|
| P2-FBA-01 | Shipment→FNSKU→unit-path audit + 4 inconsistency flags (consumes shared timeline read-only). | A/B PASS |
| P2-FBA-02 | **Bugfix:** UPS-tracking field clobbered non-UPS links → strict `findUpsLink`; FNSKU `condition` now in PATCH RETURNING + GET. | A/B/C PASS |
| P2-RPR-01 | Repair intake paperwork reachable from any step (reused BottomSheet). | A/B PASS |
| P2-RPR-02 | Repair ticket CRUD + link/unlink + manual pairing (soft-delete only). | A/B/C/D PASS |
| P2-AI-01 | OCR unresolved read → create-SKU OR flag-missing in one step. | A/B PASS |

### Polish (P3)
| Task | Outcome | Acceptance |
|---|---|---|
| P3-DS-03 | Photos library "Group by ticket" view; receiving peek-fan was already mounted. | A/B PASS |
| P3-ADM-01 | `/operations` goal-first hero + live stats + honest local-agents row (deep-links to `/studio`). | A/B/C PASS |
| P3-ADM-03 | `/calendar` page over `work_assignments` (reused `deadline_at`, no new column). | A/B PASS |

---

## 3. Verification cycle (3 adversarial audits + remediation)

**Found & fixed (wave-introduced, all in the P1-WORK-02 staff filter):**
1. 🔴 **`/api/receiving-lines?staff=` 500** — shared predicate referenced `scan_first` alias absent from the COUNT query. **Fixed:** rewritten as alias-independent `EXISTS` over `receiving_scans`.
2. 🟠 **`shipped ?staff=`** post-filtered in JS → **pushed into SQL** WHERE.
3. 🟠 **`receiving ?staff=`** on unindexed columns → **index migration written (UNAPPLIED).**
4. 🟡 IncomingTodoList poll 60s→180s; repair-service POST idempotency key added.

**Verified genuinely solid:** IDN-01 serial uniqueness & TRACE-04 bell inbox claims are真; both bugfixes fix root cause; PCK-02 migration safe + COALESCE-preserving; EventTimeline recursion bounded; shared-file seams (`work-orders/queries`, `GlobalHeader`, `audit-logs`) clean; **no SoR violation, no new serial mint.**

---

## 4. Carry-forward items that need YOU

### 4a. Before merging / using
- **`docs/security/route-permissions.json`** — regenerated to match new routes (in your git diff; reversible via `git checkout`).
- **`src/lib/migrations/2026-06-21_sku_catalog_pack_notes.sql`** — **already applied** to the DB (PCK-02). Additive `ADD COLUMN IF NOT EXISTS notes TEXT`, down-path documented.
- **`src/lib/migrations/2026-06-21_receiving_staff_filter_indexes.sql`** — **NOT applied.** Run `npm run db:migrate` before the receiving staff-filter is used on large history views (else table scan).

### 4b. Pre-existing CRITICAL tenancy leaks (NOT caused by this wave — surfaced by the audit)
These belong to your tenancy-hardening sweep; left untouched deliberately (security-sensitive, risky to auto-fix):
- `/api/repair-service` GET + PATCH — `createCrudHandler` callbacks call `getAllRepairs`/`searchRepairs`/update **without org scope**.
- `/api/shipped` GET (non-search) + PATCH — `getAllShippedOrders(...)` / `updateShippedOrderField(...)` **missing `organizationId` arg**.
- `/api/packerlogs` PUT + DELETE — no `organization_id` predicate; **delete by integer ID is cross-tenant.**
- `pending_skus` — global table (no `org_id`); the new OCR flag-missing route is a write-only enqueue (audit row is org-scoped).

---

## 5. Remaining `todo` (13) — all need your decision
| Category | Tasks |
|---|---|
| Credential / live account | P2-INT-02 (Ecwid), P2-INT-03 (eBay/Amazon), P2-INT-04, P3-ADM-04 (R2) |
| Product / auth judgment | P2-AI-02 (claims TOS), P2-AI-03 (sourcing), P3-BIZ-02→01 (signup/plans), P3-ADM-02 (global sign-in) |
| SoR-delicate | P2-INT-01 (Zoho webhook — crosses SKU-quantity boundary) |
| Opportunistic / blocked | P3-DS-02 (do as pages are touched), P2-RPR-03 (needs INT-02), P3-ADM-05 (needs flows stable) |
