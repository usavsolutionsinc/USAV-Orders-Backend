/** Centered content column for Unbox/Triage right-pane workspace chrome + body. */
export const RECEIVING_WORKSPACE_COLUMN =
  'mx-auto w-full min-w-0 max-w-[720px]';

/** Header rows (stepper, toolbar) — align with PaneHeaderActionBar + stepper track. */
export const RECEIVING_WORKSPACE_HEADER_COLUMN = `${RECEIVING_WORKSPACE_COLUMN} px-6 sm:px-8`;

/** Scroll body cards — align with LineEditPanel hero column. */
export const RECEIVING_WORKSPACE_BODY_COLUMN =
  `${RECEIVING_WORKSPACE_COLUMN} space-y-4 px-4 py-5 pb-32 sm:px-6`;
