'use client';

import { ShippedDetailsPanelContent } from '../ShippedDetailsPanelContent';
import { DetailsStackProps } from './types';

export function TechDetailsStack(props: DetailsStackProps) {
  return <ShippedDetailsPanelContent {...props} />;
}
