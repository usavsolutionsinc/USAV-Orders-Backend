/**
 * @deprecated Import `TabSwitch` / `SidebarTabSwitchChrome` from `@/design-system/components`
 * (or `@/design-system`) instead. This shim points at the canonical design-system source and
 * is kept only for backward compatibility with existing `@/components/ui/TabSwitch` imports.
 *
 * All tab-like navigation must use TabSwitch — no custom pill/button groups.
 * Variants: 'default' (gray rail, white pill) and 'upNext' (station-themed outline).
 */
export { TabSwitch, SidebarTabSwitchChrome } from '@/design-system/components/TabSwitch';
